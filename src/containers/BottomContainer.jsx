import React from "react";
import { Card } from "../components/cards";

import './style.css'
export const BottomContainer = ({children}) => {
  return (
    <div>
      {children}  
    </div>
  );
};
